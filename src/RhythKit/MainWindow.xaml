<Window x:Class="RhythKit.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:RhythKit.Controls"
        xmlns:vm="clr-namespace:RhythKit.ViewModels"
        Title="RhythKit Toolkit"
        Height="720" Width="1180"
        MinHeight="600" MinWidth="980"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:ColorsetMakerViewModel}">
            <controls:ColorsetMakerPage/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <controls:SettingsPage/>
        </DataTemplate>
    </Window.Resources>

    <Border CornerRadius="14" Background="{StaticResource BgBrush}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="2" Opacity="0.5" Color="Black"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource BgAltBrush}" CornerRadius="14,14,0,0"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                <Grid x:Name="TitleBar" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="16,8,0,8">
                        <controls:RhythKitLogo Width="200" Height="44"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                        <Button x:Name="MinimizeBtn" Style="{StaticResource WindowControlBtn}" Content="&#xE921;" Click="MinimizeBtn_Click"/>
                        <Button x:Name="MaximizeBtn" Style="{StaticResource WindowControlBtn}" Content="&#xE922;" Click="MaximizeBtn_Click"/>
                        <Button x:Name="CloseBtn" Style="{StaticResource WindowControlBtn}" Content="&#xE8BB;" Click="CloseBtn_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="230"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{StaticResource BgAltBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                    <DockPanel Margin="12,20,12,16">
                        <TextBlock DockPanel.Dock="Top" Text="TOOLKIT" Style="{StaticResource MutedText}"
                                   Margin="12,0,0,12" FontSize="11"/>
                        <Button DockPanel.Dock="Top" Style="{StaticResource NavButton}" Command="{Binding GoToColorsetCommand}"
                                Tag="🎨" Content="Colorset Maker" Margin="0,0,0,6"/>
                        <Button DockPanel.Dock="Top" Style="{StaticResource NavButton}" Command="{Binding GoToSettingsCommand}"
                                Tag="⚙️" Content="Settings" Margin="0,0,0,6"/>

                        <TextBlock DockPanel.Dock="Bottom" Text="RhythKit Toolkit" Style="{StaticResource MutedText}"
                                   HorizontalAlignment="Center" Margin="0,16,0,0" FontSize="11"/>
                    </DockPanel>
                </Border>

                <Grid Grid.Column="1">
                    <Grid x:Name="ContentHost" Margin="24"
                          RenderTransformOrigin="0.5,0.5">
                        <Grid.RenderTransform>
                            <TranslateTransform/>
                        </Grid.RenderTransform>
                        <ContentControl Content="{Binding CurrentPage}"/>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
